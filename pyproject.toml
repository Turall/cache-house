[tool.poetry]
name = "cache_house"
version = "0.1.0"
description = ""
readme = "README.rst"
license = "MIT"
homepage = "https://github.com/Turall/cache_house"
repository = "https://github.com/Turall/cache_house"
authors = ["Tural Muradov <tural.muradov@pashabank.az>"]

[tool.poetry.dependencies]
python = "^3.8"
redis = "^4.1.0"

[tool.poetry.dev-dependencies]
pytest = "^5.2"
black = {version = "^21.12b0", allow-prereleases = true}

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
